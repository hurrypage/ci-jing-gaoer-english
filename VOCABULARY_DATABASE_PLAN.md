# 词境词汇数据库与排程器方案

## 目标与边界

- **范围层**：`data/vocabulary-catalog-v1.json` 收录 3,099 条国家课程标准范围词，作为全年覆盖清单；不称为上海官方词表或上海真题词频。
- **优先层**：819 条 S 类候选词仅来自本地候选语料统计，用于候选排序；来源等级 C，不能用作正式结论。
- **精学卡层**：只有拥有词义、词性、搭配、例句、主动回忆提示及内容来源的 `ready_editor` 词条，才可进入完整精学卡。目前为第 1 周 30 条编辑学习卡。
- **个人复习层**：只存本机浏览器。包含开始日期、目标日期、已引入词、复习结果、下次日期和“太简单”状态。导出学习记录即可迁移或备份。

## 核心字段

| 层 | 关键字段 | 用途 |
| --- | --- | --- |
| VocabularyScope | `id`, `headword`, `scope_source`, `scope_level` | 确定应覆盖的范围 |
| VocabularyPriority | `priority_band`, `candidate_exam_frequency`, `priority_source` | 决定待制卡的先后；不改变真实性等级 |
| LearningCard | `card_status`, `definition_status`, `learning_content_source`, `content_boundary` | 防止未完成词条伪装成完整闪卡 |
| ReviewState | `introducedAt`, `dueAt`, `reps`, `lastGrade`, `retired` | 为单个学生安排复习和补救 |

## 排程规则

1. 每次打开课程先安排**到期复习**；遗忘或“需要复习”的词优先。
2. 到期复习超过每日上限时，暂停新增，先清理复习债务。
3. 新词只从 `ready_editor` 队列投入；课程日序固定，绝不随机抽词。
4. “太简单”不删除范围记录，只将该词移出主动复习队列；学生可在设置中恢复。
5. 其余 3,069 条范围词显示为“待制卡/快速诊断”。在未补齐释义、搭配与语境前，不计入“完整精学完成”。
6. 每日新增预算按 `ceil(剩余范围词 ÷ 剩余有效学习日)`计算。以一年、每周 5 个学习日为例，完整范围需约 14 条/日；这与 20 分钟内的深度词卡学习冲突，因此页面会如实显示这一容量缺口。

## 数据接入和审核流程

1. 先以可追溯来源补全每个候选词的义项、搭配和例句。
2. 人工审核后将状态从 `not_yet_enriched` 改为 `ready_editor`。
3. 再由排程器把它投入精学卡。
4. 上海正式试卷、题句或频次需要单独 A/B 级核验；未经核验内容不写进词卡“真题例句”字段。
